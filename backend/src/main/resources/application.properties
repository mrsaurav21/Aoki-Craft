spring.application.name=shopease

# ===============================
# SERVER (Render)
# ===============================
server.port=${PORT:8080}

# IMPORTANT: Fix OAuth redirect behind Render proxy
server.forward-headers-strategy=framework


# ===============================
# DATABASE (PostgreSQL - Neon / Render)
# ===============================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# ===============================
# MAIL (Gmail SMTP)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# ===============================
# JWT
# ===============================
jwt.auth.app=authapp
jwt.auth.secret_key=${JWT_SECRET}
jwt.auth.expires_in=3600


# ===============================
# GOOGLE OAUTH (FIXED)
# ===============================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri=https://aoki-craft-backend.onrender.com/login/oauth2/code/google
spring.security.oauth2.client.registration.google.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.google.client-name=Google

spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v3/userinfo


# ===============================
# STRIPE
# ===============================
stripe.secret=${STRIPE_SECRET_KEY}


# ===============================
# FILE UPLOAD (Bunny CDN)
# ===============================
FILE_ZONE=${FILE_ZONE}
FILE_UPLOAD_API_KEY=${FILE_UPLOAD_API_KEY}
FILE_UPLOAD_HOST_URL=${FILE_UPLOAD_HOST_URL}
